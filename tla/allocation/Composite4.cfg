\* Composite4: targeted local confirmation of the PREDICTED BUG-6 -> BUG-4
\* composite (bugs/BUG-6.md item 6): after the BUG-6 stranding, a deferred
\* admission resets rel := cur resurrecting the stranded READY tag; a later
\* request-time-TRIGGERED destroy reaches ARR full-success, swaps the
\* rel-derived state into cur, and erases the ready entry FIRING its
\* deferred notify -> permanent range leak + notify-while-tag-live, the
\* #442 class, with NO poison anywhere.
\*
\* ("4" names BUG-4; the scenario needs FIVE instances - four are provably
\* insufficient: the stranding consumes i1 (drained) and i3 (stranded), the
\* rel resurrection needs a fresh deferred create i4, and the ARR invocation
\* needs a fresh ALLOCATED instance i5, because the only other pending
\* entry (R2, oldest) drains i3's entry correctly via the oldest path.)
\*
\* Intended witness (SCRIPTED_COMPOSITE client, H=4, sizes 2,2,1,1,1;
\* TLC will find this interleaving among the explored orders):
\*   1-3. create(1)@[0,2), create(3)@[2,3), create(5)@[3,4)  - heap full.
\*   4. destroy(1) requested: deps={1}+ballistic -> deferred, R1(seq1).
\*   5. create(2) sz2: cur full; fut = cur - R1 -> fits @0 -> DEFERRED
\*      (lastSeq=1); rel := cur = {1,3,5}.
\*   6. destroy(2): deps={2} unfired -> deferred, R2(seq2); fut -= 2.
\*   7. destroy(3): deps={3} resolved -> request-time TRIGGERED:
\*      rel -= 3, fut -= 3; ARR front gate: sz2 vs 1-cell hole -> FAIL ->
\*      R3 pushed READY+defNote (seq3, cc:884-887).
\*   8. ballistic(D1) fires; EnvTriggerDestroy(1): oldest drain frees 1,
\*      unblock places 2@[0,2) (lastSeq 1 < seq(R2) 2); pendingAllocs
\*      empties; do-while stops at non-ready R2 -> R3 STRANDED READY
\*      (BUG-6 state; both cleanup sites skipped).  cur = {2,3,5}, full.
\*   9. create(4) sz1: cur full; ADA rebuild walks [R2, R3-ready] (cc:772
\*      ghost readyAtRebuild - invariant deliberately NOT checked here);
\*      fut = {5}; 4 fits -> DEFERRED (lastSeq=3); rel := cur = {2,3,5}
\*      - the stranded tag 3 is RESURRECTED into rel while R3 stays READY.
\*  10. destroy(5): deps={5} resolved -> request-time TRIGGERED:
\*      rel -= 5 (rel = {2,3}, hole [3,4)), fut -= 5; ARR: test := rel;
\*      front gate: sz1 fits @3 -> greedy places 4 -> FULL SUCCESS ->
\*      cur' := test = {2@[0,2), 3@[2,3), 4@[3,4)}; R3 ERASED, its
\*      deferred notify FIRES for instance 3.
\*      => tag 3 in cur, notifyCount[3] = 1, no pendingReleases entry:
\*      INV_NoOrphanTags VIOLATED (permanent leak of [2,3)).
\*
\* TLC flags: -deadlock (a BUG-1-shape deadlock elsewhere in the order
\*            space must not preempt the invariant hunt).
\* Expected:  FAIL: INV_NoOrphanTags (composite confirmed).
\*            INV_NoReadyWhenNoPendingAllocs / INV_NoReadyAtRebuild are the
\*            known BUG-6 markers on the path and are deliberately excluded
\*            from the battery.
\* Scale:     local; scripted terms keep the order-interleaving space small.
CONSTANTS
  HEAP_SIZE = 4
  INSTANCES = {1, 2, 3, 4, 5}
  Size <- SizesComposite
  USER_POISON = FALSE
  C1_ENABLED = TRUE
  C2_ENABLED = TRUE
  C3_ENABLED = FALSE
  CLIENT_MODE = "SCRIPTED_COMPOSITE"
  FIX_CAP = FALSE
  FIX_SWEEP = FALSE
  FIX_RPR = FALSE
INIT MCInit
NEXT MCNext
INVARIANTS
  TypeOK
  INV_NoOverlap
  INV_InBounds
  INV_Conservation
  INV_CurrentMatchesGround
  INV_TriggeredDeallocPresent
  INV_InOrderUnblockSucceeds
  INV_FutureOffsetConsistency
  INV_PoisonReplayOnlyFailsAfterPoint
  INV_NoOrphanTags
  INV_QuiescentHeapEmpty
  PROP_NotifyOnceSafety
  INV_NoDupAlloc
  SAFETY_PromisesKept
  INV_StructuralAsserts
CONSTRAINT
  SeqCtrBound
